# Schema per document

ACCOUNT
{
    name  = str,
    accid = str,

    password  = str
}

MESSAGE
{
    sender  = accid: str, // decided on account creation
    content = str,
    msgid   = str         // decided on message send,
    senton  = unix: int,
}